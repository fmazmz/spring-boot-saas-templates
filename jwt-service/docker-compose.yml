services:
  postgres:
    container_name: auth-db
    image: postgres:16
    environment:
      POSTGRES_USER: jwt
      POSTGRES_PASSWORD: jwt
      POSTGRES_DB: auth_db
    ports:
      - "5433:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: