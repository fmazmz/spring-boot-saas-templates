spring:
  application:
    name: jwt-service
  datasource:
    url: jdbc:postgresql://localhost:5433/auth_db
    username: jwt
    password: jwt
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true